Сверь факты текущей работы с wiki.

Разрешено:
- создавать и обновлять `wiki/progress/`;
- создавать технические решения в `wiki/decisions/`;
- добавлять новые записи в `wiki/log.md` строго append-only;
- переносить устаревшее в `wiki/attic/` без удаления истории.

Запрещено этой командой:
- изменять `wiki/hot.md` — это делает только `handoff`;
- редактировать `wiki/concepts/discovery.md`;
- редактировать исходный текст и ID в `wiki/audits/`.

Сохраняй ссылки на sources и покажи diff перед завершением.
